# Template-specific composition

`/UI` remains the global LOOKDO shell reference. Template-local `UI/` references may rearrange the hierarchy of reusable content blocks to fit the activity. They must not create a separate frontend. Tenant-selected colors come from `/TEMPLATE_COLOR_SYSTEM.md`.
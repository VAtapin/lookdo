# Brow template colors

The pink accent in the Brow UI reference is only the default preview. The tenant may select any valid primary color during setup and change it later. The Brow layout, booking flow, calendar and Before/After presentation must use the global semantic tenant color tokens from `/TEMPLATE_COLOR_SYSTEM.md`; no pink value may be hardcoded into Vue business logic.
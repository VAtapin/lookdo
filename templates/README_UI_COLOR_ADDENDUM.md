# UI and color addendum

Mandatory project rule: each template family has its own UI reference showing the activity-specific composition. Specialized templates with a distinct flow have their own reference too. The shared shell stays reusable.

Tenant chooses the primary app color when selecting/configuring a template. References show example palettes only. See `/TEMPLATE_COLOR_SYSTEM.md` and `/templates/UI_REFERENCE_RULE.md`.